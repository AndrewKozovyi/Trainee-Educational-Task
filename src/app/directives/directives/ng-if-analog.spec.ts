import { NgIfAnalog } from './ng-if-analog';

describe('NgIfAnalog', () => {
  it('should create an instance', () => {
    const directive = new NgIfAnalog();
    expect(directive).toBeTruthy();
  });
});
